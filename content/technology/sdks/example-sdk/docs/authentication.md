---
title: Authentication
summary: Example SDK supports Bearer token authentication.
---

<!-- Generated fixture content. Replace or regenerate intentionally in downstream repos. -->

# Authentication

Example SDK uses Bearer token authentication. Prefer the `Authorization` header over query string credentials when your client supports headers.

## Local Development

For local demos, you can disable authentication with an explicit development-only setting.
